import accept from './accept';
import cancel from './cancel';
import join from './join';
import reject from './reject';

const CRUD = {
    accept: accept,
    cancel: cancel,
    join: join,
    reject: reject
};

export default CRUD;